# 使用示例

本文档提供详细的使用示例和场景说明。

## 📖 场景 1：第一次使用

### 目标
完整体验从获取论文到生成精读报告的全流程。

### 步骤

1. **安装并配置**
   ```bash
   # 安装依赖
   pip install -r requirements.txt

   # 配置环境变量
   copy .env.example .env
   # 编辑 .env，填写 DOUBAO_API_KEY
   ```

2. **启动应用**
   ```bash
   # 双击 run.bat 或运行
   python run.py
   ```

3. **访问界面**
   - 打开浏览器：http://127.0.0.1:8000

4. **获取论文**
   - 点击"手动获取最新论文"
   - 等待 1-2 分钟（会显示进度）

5. **选择论文**
   - 浏览论文列表，阅读 AI 生成的简介
   - 勾选 6 篇感兴趣的论文

6. **生成精读**
   - 点击"生成精读报告"
   - 等待 2-3 分钟
   - 浏览器会自动打开 6 个标签页

7. **阅读报告**
   - 每个标签页显示一篇论文的详细解读
   - 包含：背景、问题、核心洞察、方法、结论等

---

## 📅 场景 2：每日工作流

### 目标
建立每天早上查看最新论文的习惯。

### 配置

1. **设置定时时间**

   编辑 `.env`：
   ```ini
   SCHEDULE_TIME=09:00  # 每天早上 9 点运行
   CHECK_ON_STARTUP=true
   ```

2. **安装开机自启动**

   右键 `install_startup.bat`，选择"以管理员身份运行"

### 使用流程

**每天早上 9:00**（自动）：
- 应用自动获取昨天的最新论文
- AI 自动生成每篇论文的 2 句话简介
- 保存到数据库

**你到办公室后**（10:00 左右）：
- 打开浏览器访问 http://127.0.0.1:8000
- 浏览今天的论文列表（约 20-30 篇）
- 快速阅读 AI 简介，选择 6 篇感兴趣的
- 点击"生成精读报告"
- 喝杯咖啡，等待 2-3 分钟
- 开始深度阅读 6 篇论文

**预计时间**：
- 浏览和选择：10-15 分钟
- 深度阅读：30-60 分钟
- **总计：40-75 分钟**

---

## 🔍 场景 3：周末补课

### 目标
周末查看本周错过的论文。

### 步骤

1. **打开应用**
   ```bash
   python run.py
   ```

2. **查看最近 7 天的论文**
   - 在界面上选择"最近 7 天"
   - 浏览所有论文

3. **筛选和阅读**
   - 可以按点赞数排序，找到最热门的论文
   - 选择 6 篇最感兴趣的
   - 生成精读报告

---

## 🎯 场景 4：按主题筛选

### 目标
只关注特定领域的论文（如 NLP、CV 等）。

### 方法 1：使用浏览器搜索

1. 打开论文列表页面
2. 按 `Ctrl+F` 打开搜索框
3. 输入关键词，如 "NLP"、"Vision"、"LLM" 等
4. 浏览器会高亮匹配的论文

### 方法 2：修改初筛 Prompt

编辑 `backend/config.py`，在 `INITIAL_SCREENING_PROMPT` 中添加领域标签：

```python
INITIAL_SCREENING_PROMPT = """请用两句话简要介绍这篇论文的核心创新点和实用价值。

论文标题：{title}
论文摘要：{abstract}

要求：
1. 第一句话说明论文解决什么问题或提出什么创新
2. 第二句话说明这个工作的价值或应用场景
3. 如果是 NLP/CV/RL/ML 等特定领域，请在开头标注 [NLP] 或 [CV] 等标签
4. 总共不超过100字
5. 用中文输出"""
```

---

## 💡 场景 5：自定义精读重点

### 目标
根据你的研究方向，定制精读报告的重点。

### 示例 1：关注实验设计

编辑 `backend/config.py`，修改 `DEEP_READING_PROMPT`：

```python
# 在 DEEP_READING_PROMPT 的 "5. 结论" 部分后添加：

**6. 实验设计详解**
- 数据集选择和预处理
- 基线模型（Baseline）选择
- 评估指标（Metrics）
- 实验设置（超参数、训练策略等）
- 消融实验（Ablation Study）结果

**7. 可复现性分析**
- 代码是否开源？
- 实验细节是否充分？
- 是否有潜在的复现难点？
```

### 示例 2：关注工程实践

```python
# 添加到 DEEP_READING_PROMPT：

**6. 工程实践价值**
- 模型复杂度（参数量、计算量）
- 训练成本（GPU 时间、数据量）
- 推理速度和资源占用
- 是否适合工业部署？
- 有哪些工程优化技巧？
```

---

## 🤖 场景 6：多人协作

### 目标
团队成员共同筛选和阅读论文。

### 方案 1：共享数据库（同一网络）

1. **服务器端配置**

   编辑 `.env`：
   ```ini
   HOST=0.0.0.0  # 允许外部访问
   PORT=8000
   ```

2. **客户端访问**

   团队成员访问：`http://服务器IP:8000`

3. **协作流程**
   - 成员 A 负责每天获取和初筛论文
   - 成员 B、C、D 各自选择感兴趣的论文
   - 每人生成精读后，可以分享链接

### 方案 2：导出和分享

1. **导出精读报告**

   在报告页面，复制内容到 Markdown 文件

2. **分享到团队**

   将报告发送到团队群组或知识库

---

## 📊 场景 7：数据分析

### 目标
分析论文趋势，了解热门方向。

### 使用数据库查询

连接到 `data/papers.db`，执行 SQL 查询：

```sql
-- 查看最近 30 天点赞数最高的论文
SELECT title, upvotes, published_at
FROM papers
WHERE fetch_date >= date('now', '-30 days')
ORDER BY upvotes DESC
LIMIT 20;

-- 统计作者出现频率
SELECT author, COUNT(*) as paper_count
FROM (
    SELECT json_each.value as author
    FROM papers, json_each(authors)
)
GROUP BY author
ORDER BY paper_count DESC
LIMIT 20;

-- 查看自己精读过的论文数量
SELECT COUNT(*) as total_readings
FROM deep_readings;
```

### 导出数据

```bash
# 导出所有论文到 CSV
sqlite3 data/papers.db -header -csv "SELECT * FROM papers;" > papers.csv

# 导出精读记录
sqlite3 data/papers.db -header -csv "SELECT * FROM deep_readings;" > readings.csv
```

---

## 🔧 场景 8：API 集成

### 目标
将论文精读功能集成到其他应用。

### API 端点

```bash
# 获取最近 7 天的论文
GET http://127.0.0.1:8000/api/papers?days=7

# 获取单篇论文
GET http://127.0.0.1:8000/api/paper/{arxiv_id}

# 选择论文
POST http://127.0.0.1:8000/api/select-papers
Content-Type: application/json
{
  "arxiv_ids": ["2023.12345", "2023.12346"]
}

# 生成精读
POST http://127.0.0.1:8000/api/generate-deep-readings

# 获取精读结果
GET http://127.0.0.1:8000/api/deep-reading/{arxiv_id}

# 手动触发论文获取
POST http://127.0.0.1:8000/api/manual-fetch
```

### Python 示例

```python
import requests

# 获取论文列表
response = requests.get("http://127.0.0.1:8000/api/papers?days=7")
papers = response.json()["papers"]

# 选择前 6 篇
selected_ids = [p["arxiv_id"] for p in papers[:6]]

# 提交选择
requests.post(
    "http://127.0.0.1:8000/api/select-papers",
    json={"arxiv_ids": selected_ids}
)

# 生成精读
requests.post("http://127.0.0.1:8000/api/generate-deep-readings")
```

---

## 💰 场景 9：成本优化

### 目标
减少 API 调用成本。

### 策略 1：使用更便宜的模型

编辑 `.env`：
```ini
# 初筛用 lite 模型（便宜）
DOUBAO_CHEAP_MODEL=doubao-lite-4k

# 精读用 pro 模型（高质量）
DOUBAO_PREMIUM_MODEL=doubao-pro-32k
```

### 策略 2：减少初筛数量

编辑 `.env`：
```ini
# 每天最多获取 30 篇（而不是 50 篇）
MAX_PAPERS_PER_DAY=30
```

### 策略 3：缓存已读论文

应用会自动将初筛结果存入数据库，避免重复调用 API。

---

## 🚀 高级技巧

### 技巧 1：快捷键

- `Ctrl+F`：在论文列表中搜索关键词
- `Ctrl+W`：关闭精读报告标签页
- `Ctrl+Shift+T`：重新打开关闭的标签页

### 技巧 2：浏览器书签

将常用链接添加到书签：
- 论文列表：http://127.0.0.1:8000
- API 文档：http://127.0.0.1:8000/docs

### 技巧 3：定制 CSS

如果想修改界面样式，编辑 `frontend/static/style.css`

### 技巧 4：批量导出

使用脚本批量导出精读报告为 Markdown 文件。

---

**更多问题？查看 [README.md](README.md) 或提交 Issue！**
